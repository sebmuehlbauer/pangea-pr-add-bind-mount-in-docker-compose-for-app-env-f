version: '3.8'

services:
  app:
    build: .
    container_name: acm-meeting-records-app
    volumes:
      - .:/app
      - .env:/app/.env:ro
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped

networks:
  default:
    name: acm-network